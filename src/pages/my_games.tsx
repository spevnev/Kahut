import React from 'react';
import { NextPage } from 'next';

const UsersGames: NextPage = () => {
    return <div>{/* My games + ratings/statistics(or in profile) + create */}</div>;
};

export default UsersGames;
