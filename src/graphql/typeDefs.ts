import { gql } from '@apollo/client';

const typeDefs = gql`
    scalar Null

    # type Query {
    # }

    # type Mutation {
    # }

    # type Subscription {
    # }
`;

export default typeDefs;
