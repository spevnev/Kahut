import type {NextPage} from "next";
import styled from "styled-components";

const Container = styled.div`

`;

const Home: NextPage = () => {
	return (
		<Container>

		</Container>
	);
};

export default Home;